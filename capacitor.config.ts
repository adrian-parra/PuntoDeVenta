import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.puntoventa.app',
  appName: 'punto_de_venta',
  webDir: 'dist/punto_de_venta',
  bundledWebRuntime: false
};

export default config;
